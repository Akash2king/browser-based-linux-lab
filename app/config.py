import os

# Terraform configuration
TERRAFORM_DIR = os.path.join(os.getcwd(), "terraform")

# VM lifetime in seconds
LIFETIME_SECONDS = 600  # 10 minutes

# VM Configuration
VM_URL = "https://localhost:6901"
